# Global runtime settings for certify-reverse
DNS_PROVIDER=desec
DNS_TOKEN=REPLACE_WITH_DNS_API_TOKEN
ACME_EMAIL=admin@example.com
DOMAIN=example.com
# Optional. Use latest by default, or pin (e.g., v2.10.2)
CADDY_VERSION=latest
